# 変数

変数は特定の値を示す名前です。`const` や `let` を使って定義します。

```js
const example = "some string";
```

`const` で宣言した変数には再代入できません。`let` は再代入できます。
意図しない再代入を防ぐため、基本的には `const` を使い、再代入が必要なときだけ `let` を使いましょう。

## やってみよう

変数 `example` を宣言し、文字列 `"some string"` を代入してください。

`console.log(example)` でデバッグ出力できます。