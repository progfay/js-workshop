# はじめの一歩

このアプリでは、エディタにコードを書き、**実行**ボタンで採点します。

採点は「テストケースを1つずつ実行し、`throw` されなければ合格」という方式です。
`console.log()` はデバッグ用として使えます。出力は実行結果のパネルに表示されます。

## やってみよう

文字列 `"hello"` を表す変数 `message` を定義してください。

```js
const message = "hello";
```

書けたら**実行**して、全テストケースが緑になれば正解です。