# 配列

配列は値を順番に並べたものです。角括弧で書きます。

```js
const pizzaToppings = ["tomato sauce", "cheese", "pepperoni"];
```

## やってみよう

変数 `pizzaToppings` に、次の3要素の配列を代入してください。

```js
["tomato sauce", "cheese", "pepperoni"]
```

`console.log(pizzaToppings)` でデバッグ出力できます。