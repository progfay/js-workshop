# 数値丸め

基本的な計算には `+`, `-`, `*`, `/`, `%` を使います。
より複雑な数値処理には `Math` オブジェクトを使います。

`Math.round()` は引数を四捨五入して最も近い整数を返します。

```js
Math.round(0.5); // -> 1
```

## やってみよう

1. 変数 `roundUp` に小数 `1.5` を代入してください。
2. 変数 `rounded` に `Math.round(roundUp)` の結果を代入してください。

`console.log(rounded)` でデバッグ出力できます。