# 配列のフィルター

`Array.prototype.filter` は、コールバックが `true` を返した要素だけを集めた**新しい配列**を返します。

```js
[1, 2, 3, 4].filter((n) => n % 2 === 0); // -> [2, 4]
```

## やってみよう

数値の配列 `numbers` を受け取り、**偶数だけ**を順番を保って返す関数 `evens` を実装してください。

```js
evens([1, 2, 3, 4, 5, 6]); // -> [2, 4, 6]
```