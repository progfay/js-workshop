# 数値を文字列に

数値を文字列に変換したいときは `.toString()` メソッドを使います。

```js
const number = 256;
const numString = number.toString(); // -> "256"
```

## やってみよう

1. 変数 `number` に数値 `128` を代入してください。
2. 変数 `numString` に `number.toString()` の結果を代入してください。

`console.log(numString)` でデバッグ出力できます。