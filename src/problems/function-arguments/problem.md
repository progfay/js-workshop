# 関数の引数

関数は複数の引数を受け取れます。引数は左から順に対応します。

```js
function add(a, b) {
  return a + b;
}
add(2, 3); // -> 5
```

## やってみよう

3つの数値 `a`, `b`, `c` を受け取り、`b * c + a` を返す関数 `math` を実装してください。

```js
math(53, 61, 67); // -> 4140  (61 * 67 + 53)
```

掛け算が足し算より先に計算される点に注意してください。